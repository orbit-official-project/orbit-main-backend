smtp:
  host: smtp.naver.com
  email: spok_nb@naver.com
  password:
  port: 465

proxyServer:
  sites-enabled: C:\Users\joyou\Desktop

spring:
  redis:
    host: localhost
    port: 6379

  jpa:
    database-platform: org.hibernate.dialect.MySQL57Dialect

    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: #

  profiles:
    active: jwt,datasource